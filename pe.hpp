#ifndef PE_HPP_
#define PE_HPP_

// Use
// g++ -xc++-header pe.hpp --std=c++20 -fno-diagnostics-color -O3 -march=native
// -mtune=native -fopenmp -pthread -static
// to generate pe.hpp.gch
#include <pe>

#endif
